# drydock v0.1.0 — public release plan

Briefing for the next Claude session that will polish this project into a
shareable open-source release under `sideralith/drydock` on GitHub.

> **Status**: Pre-release. The infrastructure works end-to-end on the original
> developer's machine (`rai`, UID 1000, WSL2). This document describes what
> needs to change to make it shareable, with constraints, validation, and
> non-goals explicit.

---

## What drydock is, in one paragraph

A defense-in-depth containerized sandbox for Claude Code. It runs Claude in a
Debian 12 slim container with `/var/run/docker.sock` bind-mounted (DooD), so
the agent has full access to the user's Docker workflow against existing
project stacks, but **no access** to `~/.ssh`, `~/.aws`, `~/.gnupg`,
`~/.kube`, or other unrelated home paths. The agent's own hooks are
RO-mounted (it can't self-edit guardrails). Engram memory and Claude Code's
local state run from container-specific directories (`~/.engram-container/`,
`~/.claude-container/`), so host and container Claude sessions can run in
parallel without SQLite/WAL contention.

Multi-project: one image, many projects. CLI tool `drydock` resolves
`PROJECT_DIR` from cwd or arg at invocation time.

## Current state (snapshot)

### What works

- ✓ Container builds (`drydock build`).
- ✓ `drydock init <dir>` creates `.claude/settings.json` baseline per project.
- ✓ `drydock` runs Claude in cwd's project.
- ✓ Auto-detects `docs/` 9P drvfs sub-mount on WSL2 and applies the
  conditional overlay (`docker-compose.docs.yml`).
- ✓ Auto-triggers `drydock setup` (host-side init) on first run if missing.
- ✓ Engram split: container has its own DB (`~/.engram-container/`);
  validated with T1 (search existing memory), T2 (save + find dummy),
  T3 (host DB unmodified after container session).
- ✓ **Config split is complete**: BOTH `~/.claude/` (dir → `~/.claude-container/`)
  AND `~/.claude.json` (file → `~/.claude-container.json`) are mounted. The
  `.claude.json` mount was added 2026-05-12 after discovering that without it
  Claude Code inside the container couldn't find its config file, created a
  fresh ephemeral one, and "settings didn't persist" across sessions. Now
  the container sees the project list, onboarding flags, MCP servers, OAuth.
- ✓ Verification matrix: 25/25 tests pass on serendipilink workspace (note:
  the matrix predates the `.claude.json` fix — re-running it would benefit
  from a new test asserting `~/.claude.json` is visible inside the container
  with the project list intact).

### What's hardcoded to the original developer

- `/home/rai/` literal in multiple files:
  - `Dockerfile` (line: `ENV PATH=/home/rai/.local/bin:...`)
  - `docker-compose.yml` (mount paths, incl. `~/.claude-container.json`)
  - `docker-compose.docs.yml`
  - `bin/drydock` defaults (`HOST_CLAUDE`, `CONTAINER_CLAUDE`,
    `HOST_CLAUDE_JSON`, `CONTAINER_CLAUDE_JSON`, `HOST_ENGRAM`,
    `CONTAINER_ENGRAM`)
- `USER_NAME=rai` build arg default in Dockerfile.
- README acknowledgments + branding pegged to Sideralith.

### What's WSL2-specific (acceptable for v0.1.0; document caveats)

- `/var/run/docker.sock` path. Docker Desktop on Mac sometimes uses
  `~/.docker/run/docker.sock` symlinked from `/var/run/`. May work; needs
  verification.
- 9P drvfs detection is WSL2/Windows-specific. On Linux native, the check
  returns false and the overlay doesn't activate — correct behavior.
- gRPC FUSE bind-mount perf on Mac is different. Not tested.

## v0.1.0 acceptance criteria

The release is "ready to share" when:

1. **No hardcoded user paths**: a fresh user with a different `$HOME` and
   different `id -un` can clone the repo, run `install.sh`, and have it work
   end-to-end without manual file edits.
2. **LICENSE present** (MIT, already in repo).
3. **CONTRIBUTING.md present** with: how to file issues, how to PR, code
   style (shellcheck pass), where to discuss.
4. **`install.sh` works** via `curl -fsSL <url> | bash` AND via manual
   clone+inspect+run.
5. **`examples/` directory** with at least 2 minimal templates:
   - `examples/web-stack/` — a docker-compose project with backend + db
   - `examples/minimal/` — bare project with just `.claude/settings.json`
6. **shellcheck CI** passing in `.github/workflows/lint.yml`.
7. **README depersonalized**: Sideralith mention moves to footer/credit; the
   header is about drydock itself.
8. **Tag `v0.1.0` published** on GitHub with release notes.
9. **Engram is fully optional**: users without engram installed see zero
   error noise. `drydock setup`, `drydock run`, and `drydock` work cleanly
   without engram binary present. Engram mount activates only if detected.
10. **`CLAUDE.md` present at repo root**: drydock's own AI-agent constitution
    written in Mercadona-style (rule + WHY + consequence per hard constraint).
    Encodes the load-bearing decisions so future Claude sessions working on
    drydock have non-negotiable context.

## Constraints (DO NOT change without discussion)

These are load-bearing decisions from prior work. Re-deciding requires
specific reason:

- **Threat model = A**: defense against accidents, not adversarial. Don't add
  socket-proxy in v0.1.0 — that's v0.2.0 if there's demand.
- **Docker socket bind-mount**: critical for the use case (`make shell-api`
  works). Don't replace with HTTP proxy in v0.1.0.
- **Engram split via separate `~/.engram-container/`**: not via shared DB
  with locks. WSL2 9P fcntl unreliability is the reason.
- **Hooks RO overlay**: `~/.claude/hooks/` mounted `:ro` on top of the
  container's `.claude` mount. Keep this.
- **`drydock init [DIR]` = per-project** (git-style). `drydock setup` =
  host-side, auto-triggered. Don't revert this naming.
- **`__HOME__` placeholder** in `templates/default-settings.json` — used by
  `drydock init` with `sed` substitution. Keep this pattern.
- **The serendipilink branch `dev-post-dockerized-claude`** still has
  `.claude/settings.json` with substituted `/home/rai/...` paths in the deny
  list. That's intentional — that branch is pinned to the original setup.
  Don't backport changes there.

## Non-goals for v0.1.0

- Adapter pattern for other AI agents (Codex, OpenCode, pi.dev). v0.2.0.
- macOS first-class support. Best-effort, document limitations.
- Windows native (non-WSL2). Out of scope.
- Web dashboard / TUI. Out of scope.
- Auto-update mechanism. Out of scope.
- Docker socket proxy integration. v0.2.0.

## Suggested SDD breakdown

The 10 acceptance points map roughly to these SDD changes (one per PR):

| # | Change | Estimated effort |
|---|---|---|
| 0 | ✅ **landed on `main`** — `testing-foundation` — refactor de testeabilidad de bin/drydock a lib/*.sh (dispatcher delgado + main()/source-guard + seams DOCKER y MOUNTS_FILE), suite bats-core, shellcheck, GitHub Actions CI; 3 PRs encadenados | ~3-3.5h |
| 1 | ✅ **landed on `main`** — `parameterize-paths` — replace `/home/rai` literals with `${HOME}`; container user from `id -un` (build arg threaded via `export_compose_env`). **Scope ended up smaller than estimated**: `#0 testing-foundation` already rewrote `lib/paths.sh`'s `HOST_*`/`CONTAINER_*` constants as `"$HOME/.claude"` etc., so #1 only touched `Dockerfile` + `docker-compose.yml` + `lib/compose.sh::export_compose_env()` + 1 `cmd_build` arg + tests. Known limitation: assumes `$HOME == /home/$(id -un)` (macOS `/Users/...` etc. out of scope for v0.1.0). | ~1h (actual ~45min) |
| 1.5 | ✅ **landed on `main`** — `optional-git-credentials` — opt-in, CLI-detected compose overlays so the containerized agent can `git push` over SSH (`docker-compose.ssh.yml` ← `~/.config/drydock/keys/<project>_deploy`, `GIT_SSH_COMMAND`, github host key pinned in the image) and make GitHub-Verified commits (`docker-compose.gpg.yml` ← `~/.config/drydock/signing/`, a throwaway sandbox GPG key, `GNUPGHOME` + `GIT_CONFIG_*`). **Never** mounts `~/.ssh` / `~/.gnupg`; credential material lives in drydock's own `~/.config/drydock/` namespace; no entrypoint (env vars only); default = unsigned commits in the box (image `ENV` turns `commit.gpgsign` off). `+ openssh-client` in the image; `+ Read/Edit/Write(__HOME__/.config/drydock/**)` and `Bash(git push --force*\|-f*\|origin :*)` denies in the template. **Depended on #1** (overlays need `${HOME}`/`${USER_NAME}` interpolation). | ~1.5–2h |
| 1.7 | ✅ **landed on `main`** — `cli-qol` — `drydock [run\|shell] [DIR] [-- ARGS…]` passthrough (so `drydock run -- --resume "session"` works; plus a `drydock -- ARGS` shorthand) + `drydock init --update` (merges new template deny entries into an existing `.claude/settings.json` via `jq`, preserving customizations — needed because `drydock init` is otherwise idempotent and won't refresh already-configured projects). Tests: `test/passthrough.bats` (new) + `test/init.bats` additions. | ~1h |
| 2 | ✅ **landed on `main`** — `engram-optional` — auto-detect engram binary (`command -v engram` + `host_is_linux()` — macOS Mach-O can't run in the container); conditional `docker-compose.engram.yml` overlay replacing the unconditional base mount; `drydock setup`/`ensure_runtime_dirs` engram-mode-aware; MCP filter removes the engram server from container config (`cmd_setup`/`cmd_sync`) when not usable (no startup noise — AC #9); opt-in isolated-vs-shared DB topology (`~/.config/drydock/engram-shared` sentinel; force-isolated safety downgrade on WSL2/macOS via `host_fs_locks_unreliable()`; `DRYDOCK_ENGRAM_SHARED=force` override); `host_fs_locks_unreliable()`/`host_is_linux()` helpers with `OSRELEASE_FILE`/`UNAME` seams; doc-fix: the false "engram is a Linux ELF" claim (engram is a cross-platform Go binary). | ~2.5h (actual ~6h incl. cross-platform redesign) |
| 3 | ✅ **landed on `main`** — `claude-md` — `CLAUDE.md` at repo root in Mercadona-style (Rule + Why + Consequence per hard constraint); 7 architectural invariants + Code/Tooling Conventions + Threat Model Boundary + Tracking-Contribution + Personality. 220 lines. Neutral English. Single-PR, doc-only. | ~1.5h (actual ~1h, mostly spec/design planning) |
| 4 | ✅ **landed on `main`** — `add-license-contributing` — LICENSE was already present (MIT) per AC #2; this change extended `CONTRIBUTING.md` from 108 → 196 lines with 5 new H2 sections: How to file an issue, How to submit a PR, Code style (all 3 CI lint gates: shellcheck + shfmt + bats), Where to discuss (Issues-only for v0.1.0), Sharing project memory with engram (optional). Reading-flow ToC + stub-removal. Cross-references CLAUDE.md §5 / §3 / INV-4 (no duplication of commit-conventions). Single-PR doc-only. | ~45min (actual ~30min) |
| 5 | ✅ **landed on `main`** — `install-script` — `install.sh` at repo root (249 lines, pure bash, no external deps): supports `curl -fsSL <URL>/install.sh | bash` AND `git clone && ./install.sh`; prereq check (docker, docker compose v2, git, jq with per-tool Linux/macOS install hints on miss); idempotent re-install (skip clone if `.git` exists, never `git pull` silently); 7-row symlink-conflict matrix; TTY-aware output (unicode boxed header + green ✓ in TTY, plain `[OK]` in non-TTY, NO_COLOR honored); 4 env-var seams (DRYDOCK_INSTALL_DIR/BIN_DIR/BRANCH/REPO_URL); bash-3.2 portable for macOS. Plus `test/install.bats` (6 cases via env-var test seams) + CI workflow extension. | ~1.5h (actual ~3h incl. 1 verify-fix round) |
| 6 | `examples-directory` — 2 minimal example projects | ~1h |
| 7 | ~~`shellcheck-ci`~~ — absorbido en #0 testing-foundation | — |
| 8 | `readme-depersonalize` — move Sideralith refs to footer; lead with drydock value prop; add "Using drydock without engram" section | ~45min |
| 9 | `release-v0.1.0` — tag, GitHub release notes, install URLs work | ~30min |
| 10 | `unreal-template` (optional, can defer) — `examples/unreal/` skeleton with denies for `Saved/`, `Intermediate/`, `Build/`, `Binaries/` | ~30min |

Total estimated: **~12-13h linear** (≈ one focused workday + a half).

> **Orden obligatorio (histórico)**: #0 testing-foundation debía aterrizar ANTES que #1 parameterize-paths — ya pasó (#0 en `main` desde `58cab9b`, #1 desde `39bb8ca`). #1.5 dependía de #1; también aterrizó. Nada bloquea #2 en adelante.

## Progress (as of 2026-05-14)

- ✅ **#0 `testing-foundation`** — `main` @ `58cab9b` (bats suite + shellcheck + shfmt + CI workflows; `bin/drydock` → thin dispatcher sourcing `lib/{common,paths,compose,commands}.sh`).
- ✅ **#1 `parameterize-paths`** — `main` @ `39bb8ca`.
- ✅ **#1.5 `optional-git-credentials`** — `main` @ `66958bc` (opt-in SSH deploy-key + sandbox GPG overlays; new `~/.config/drydock/**` + force-push denies in the template).
- ✅ **#1.7 `cli-qol`** — `main` @ `9f116cf` (`-- ARGS` passthrough in run/shell + `drydock init --update`).
- ✅ **`github-token-passthrough`** (ad-hoc, not a planned SDD item) — `main` @ `841c97f` (`environment: [- GITHUB_PERSONAL_ACCESS_TOKEN]` in the base compose so the GitHub MCP server / `gh` PAT auth work inside the container; `~/.config/gh:rw` was already mounted so it widens nothing).
- ✅ **#2 `engram-optional`** — `main` @ `ba74b47` (engram fully optional: host detection + conditional `docker-compose.engram.yml` overlay + mode-aware `cmd_setup`/`ensure_runtime_dirs` + MCP-filter for zero error noise + 4-state `cmd_status` + opt-in shared DB topology with WSL2/macOS force-isolated safety gate + `host_fs_locks_unreliable()`/`host_is_linux()` helpers + the "engram is a Linux ELF" doc-fix; full SDD cycle, verified PASS-WITH-WARNINGS all resolved).
- ✅ **#3 `claude-md`** — `main` @ `d221600` (drydock's AI-agent constitution: Mercadona-pattern markdown — Rule + Why + Consequence — encoding the 7 load-bearing architectural invariants: credential isolation in `~/.config/drydock/`, container-state split via `~/.{claude,engram}-container/`, hooks RO overlay, engram optional, engram shared-mode opt-in + force-isolated on unreliable-lock filesystems, DooD = root-equivalent, threat model A. 220 lines, neutral English, 6 sections per the briefing skeleton. Verified PASS, 0 critical/warnings, 1 non-blocking suggestion (§1 at line ceiling). Full SDD cycle: proposal #992 → spec #993 → design #994 → tasks #995 → apply-progress #996 → verify-report #997 → archive #998).
- ✅ **#4 `add-license-contributing`** — `main` @ `dc29e52` (extended `CONTRIBUTING.md` from 108 → 196 lines: 5 new H2 sections — How to file an issue, How to submit a PR, Code style with all 3 CI lint gates documented, Where to discuss, Sharing project memory with engram (optional) — plus a 7-bullet reading-flow ToC. Stub paragraph removed. Cross-references CLAUDE.md §5 / §3 / INV-4 instead of duplicating commit-conventions table. Verified PASS, 0 findings across 28 spec requirements. Full SDD cycle: proposal #1002 → spec #1003 → design #1004 → tasks #1005 → apply-progress #1006 → verify-report #1007 → archive-report).
- ✅ **#5 `install-script`** — `main` @ `ac656e8` (de0ae69 initial apply + ac656e8 verify-fix). `install.sh` at repo root: 249 lines, pure bash, both curl-pipe and manual install paths, 4 prereqs with per-tool Linux/macOS hints, idempotent re-install, 7-row symlink-conflict matrix, TTY-aware output (boxed header + green ✓ in TTY / plain [OK] in non-TTY / NO_COLOR honored), 4 env-var seams. test/install.bats (6 cases, 150/150 bats green). Verified PASS-WITH-WARNINGS (1 verify-fix round resolved C1 + W1 + W2; 4 SUGGESTIONs deferred as docs-only or maintainer-manual). Full SDD cycle: proposal #1012 → spec #1014 → design #1013 (updated this archive) → tasks #1015 → apply-progress #1016 → verify-report #1017 → archive #<NEW>.
- **`main` is at `ac656e8`**; `bats test/` → 150/150; `shellcheck`/`shfmt` clean. Nothing pushed (no GitHub remote yet — push `main` when the org/remote exists).
- ⚠️ **`drydock init` is idempotent** — it won't touch an existing `.claude/settings.json`. The new template denies (`Read/Edit/Write(__HOME__/.config/drydock/**)`, `Bash(git push --force*\|-f*\|origin :*)`) only reach a project when you `drydock init` it for the first time, OR run **`drydock init --update <DIR>`** (added by #1.7 — merges the new template denies into the existing file, preserving customizations). Already-configured projects (serendipilink, this repo, etc.) need `drydock init --update`.
- Next: **#6 `examples-directory`** — at least 2 minimal example projects: `examples/web-stack/` (docker-compose backend + db) and `examples/minimal/` (bare `.claude/settings.json` only). AC #5. Then **#8 `readme-depersonalize`**, **#9 `release-v0.1.0`** (depends on GitHub remote existing), **#10 `unreal-template`** (optional).

> SDD artifacts for the landed changes live in engram (topic keys `sdd/<change>/{explore,proposal,spec,design,tasks,verify-report,archive-report}` for `testing-foundation`, `parameterize-paths`, `optional-git-credentials`, `engram-optional` [also `apply-progress`]; `sdd/cli-qol/plan` for #1.7). `engram-optional` artifacts: explore #969, proposal #971, spec #974, design #975, tasks #977, apply-progress #978, verify-report #980, archive-report #982.

## CLAUDE.md content guide (for SDD #3)

drydock's `CLAUDE.md` is the **constitution** for any AI agent working on
drydock itself (and for human contributors who want to understand non-obvious
constraints). It should follow the **Mercadona pattern** used in serendipilink
(engram memory #870, #872): each hard rule has three parts.

### The pattern

```markdown
- **Rule**: [the constraint, stated as a verb-imperative or a forbidden behavior]
  - **Why**: [the technical or product reason — not "because it's good" but
    "because X breaks if not"]
  - **Consequence of violating**: [concrete bad outcome with a real example
    when possible]
```

Goal: convert **documentation** (descriptive, ignorable) into **constraints**
(prescriptive with reasoning the reader can judge edge cases against).

### Worked example (synthetic — the structure, not real content)

This is what a Mercadona-style rule looks like fleshed out. Synthetic example
(a generic PHP convention, not drydock-specific) so it's safe to study without
needing the serendipilink reference file:

```markdown
### Strict Types (PHP)

- **Rule**: Every PHP file MUST start with `declare(strict_types=1);`.
  - **Why**: PHP coerces types silently (`"5" == 5` is `true`), masking shape
    bugs at trust boundaries (HTTP input, DB rows, queue payloads). Strict
    types convert silent coercion bugs into compile-time / parse-time errors.
  - **Consequence of violating**: latent runtime errors at boundaries; refactor
    cost grows over time; harder to layer static analysis later.
```

Note the texture: the WHY is *specific* ("`"5" == 5` is `true`"), not vague
("type safety is good"). The consequence names *what* goes wrong and *when*
("at boundaries", "over time"). A reader hitting an edge case can reason from
the WHY rather than blindly following the rule.

### How to reach the canonical real example

The serendipilink `CLAUDE.md` (291 lines) is the canonical real-world
application of this pattern. **It is NOT accessible from inside a drydock
container** (only `${PROJECT_DIR}` = `~/git/drydock` is mounted, not other
project trees). To study it, you have options:

1. **Recommended**: just use this briefing — the pattern and a worked example
   are above; the section skeleton below is enough to write drydock's CLAUDE.md.
2. Pull engram memories #870 + #872 (these PREDATE the engram-container init
   copy, so they ARE in `~/.engram-container/engram.db`):
   ```python
   mem_get_observation(id: 870)   # full session summary describing the pattern application
   mem_get_observation(id: 872)   # the follow-up session
   ```
   These don't need a `project` filter — `mem_get_observation` fetches by ID.
3. If you really want the verbatim file: run this SDD on **host Claude** (not
   the drydock container), where `/home/rai/git/serendipilink/CLAUDE.md` is
   directly readable. Trade-off: lose the eat-your-own-dogfood of working on
   drydock inside drydock.

### Sections that drydock's CLAUDE.md should cover

Suggested skeleton (adapt to what's load-bearing for drydock specifically):

1. **Project identity** — what drydock is, threat model A (accidents not
   adversarial), explicit non-goals (no adversarial sandbox, no Mac
   first-class without testing, no Windows native).

2. **Architectural invariants** (each rule + WHY + consequence):
   - `~/.claude/hooks/` MUST be mounted `:ro` — agent can't self-edit
     guardrails. Why: otherwise the protection layer is illusory under
     adversarial behavior + bug-prone behavior. Consequence: lose the
     non-negotiable defense of tier-1 hook system.
   - Engram + Claude state run from `~/.{engram,claude}-container/` (NOT
     host's `~/.claude/`). Why: SQLite/WAL race risk over WSL2 9P fcntl
     unreliability + OAuth refresh contention. Consequence: data corruption
     or last-writer-wins lockout.
   - Docker socket bind-mount = root-equivalent on host. NEVER claim
     adversarial isolation. Why: a process with socket access can
     `docker run -v /:/host --privileged`. Consequence: false-sense-of-
     security + onboarding contributors with wrong threat model.
   - Engram is **optional**. drydock must run cleanly without it. Why:
     reduces barrier-to-entry for contributors; users may prefer other MCP
     memory systems. Consequence: lock-in to one specific MCP plugin =
     niche tool, not portable infrastructure.

3. **Code/tooling conventions**:
   - Bash: `set -euo pipefail` in every script. No `eval` over user input.
     No `bash -c "$untrusted"`. Why: bash is unforgiving; one bad expansion
     = subtle bug at scale. Consequence: production-impacting CLI bugs.
   - Compose: parameterize all paths via env vars, never hardcode paths from
     a specific user's home. Why: drydock is multi-user infrastructure.
     Consequence: blocks contributors entirely.
   - Docker: no daemon-in-container. Image stays minimal. Why: DooD via
     socket is the contract; running a nested daemon defeats the model.
     Consequence: breaks `make shell-api` against host stack.

4. **Threat model boundary (explicit)**:
   - drydock defends against **accidents**, not **adversaries**.
   - Anyone proposing adversarial protections (socket-proxy, gVisor,
     namespace isolation) must justify against the use case + show maintenance
     cost. Why: scope creep destroys minimal tools. Consequence: drydock
     becomes a sandbox-platform meta-project, drifts from its niche.

5. **Tracking & contribution**:
   - GitHub issues + PRs OK (unlike serendipilink which uses docs-only). Why:
     drydock IS an open-source community tool — issue discoverability matters
     for contributors finding bugs.
   - Conventional commits, NO `Co-Authored-By`, NO `--no-verify`.

6. **Personality (optional)**: Spanish rioplatense / voseo if Rai is the
   primary maintainer. Warm but direct. (Tone reference: serendipilink's
   CLAUDE.md Personality/Tone sections — readable only on host Claude, not
   in the drydock container. For a public OSS project, consider a more
   neutral English-default tone; the maintainer decides.)

### What to NOT put in drydock's CLAUDE.md

- Anything project-specific to one user (paths, secrets, personal preferences
  beyond tone).
- "Aspirational" rules without enforcement. If a rule exists, the CI should
  enforce it or the rule shouldn't be there.
- Long-form architecture explanation (that belongs in `README.md` or in
  this `docs/` directory).

### Reference memories

Before writing drydock's CLAUDE.md, the next Claude session can pull these.
Both predate the engram-container init copy → they ARE in the container's
`~/.engram/engram.db`, so `mem_get_observation` works from inside the drydock
container without any project filter:

```python
mem_get_observation(id: 870)   # full session summary describing the pattern application
mem_get_observation(id: 872)   # the follow-up session that extended it
```

If you use `mem_search` instead, it filters by current project — pass
`project: "serendipilink"` explicitly. But the worked example + skeleton in
*this* briefing are sufficient on their own; the memories add texture, not
load-bearing facts.

**Do NOT rely on reading `/home/rai/git/serendipilink/CLAUDE.md` directly** —
that file is on the serendipilink project tree, which is NOT mounted into the
drydock container. It's readable only if you run this SDD on host Claude.

## Engram team memory pattern (for SDD #4 / CONTRIBUTING.md)

drydock should ship with a documented pattern for contributors to share
engram memories without requiring central infrastructure. Engram supports
this natively via the `sync` subcommand.

### How it works

```bash
# Maintainer: after closing an architectural decision relevant to drydock:
cd ~/git/drydock
engram sync --project drydock              # creates a chunk in .engram/
git add .engram/
git commit -m "sync engram memories: <topic>"
git push

# Contributor (who uses engram):
git pull
engram sync --import                       # absorbs new chunks into their local DB
# Now `engram search` returns the maintainer's drydock memories in their tool.

# Contributor (who does NOT use engram):
# .engram/ directory is ignored — drydock works fine without it.
```

### Implementation notes for SDD #4

- `CONTRIBUTING.md` documents this pattern as **optional**.
- The repo commits `.engram/` chunks to git. They're text-based / binary-
  diffable. Small overhead per commit.
- `.gitignore` does NOT exclude `.engram/`. Default `git add .` picks it up.
- `examples/` directory does NOT include `.engram/` — that's project-level,
  not per-example.

### What this is NOT

- Not "engram cloud sync" — that requires self-hosted engram server. Out of
  scope for v0.1.0.
- Not a hard requirement — drydock works for users without engram entirely.

## Files of interest

- `Dockerfile` — base image definition
- `docker-compose.yml` — base mounts (parameterizable)
- `docker-compose.docs.yml` — 9P overlay
- `bin/drydock` — CLI (largest file; most logic lives here)
- `templates/default-settings.json` — baseline deny list template
- `README.md` — user-facing docs (already comprehensive; needs depersonalize)
- `LICENSE` — MIT (already in)
- `.gitignore` — basic OS/editor junk (already in)

## Engram context to recall

**Important caveat about cross-project search**: `engram search` (and the MCP
tool `mem_search`) **filters by current project by default**, auto-detected
from cwd / git remote. Empirically validated 2026-05-11: from cwd
`~/git/drydock`, a default `mem_search` returns nothing for serendipilink-tagged
memories. **You MUST pass `project: "serendipilink"` explicitly** (or
`--project serendipilink` for CLI) to access these.

Engram CLI does NOT have a "search all projects" flag in v1.15.10. The MCP
tool `mem_search` exposes an optional `project` parameter — pass it
explicitly when reaching upstream context.

**Primary source of truth**: *this briefing document*. The "CLAUDE.md content
guide" section above has the Mercadona pattern + a worked example + the
section skeleton — that's enough to write drydock's CLAUDE.md. The
serendipilink `CLAUDE.md` (291 lines) is the canonical real-world example,
**but it's NOT accessible from inside a drydock container** (only
`${PROJECT_DIR}` is mounted, not other project trees). Engram memories below
add texture. None of them are strictly required if you trust the briefing.

In the new Claude session, after `/sdd-init`, for each upstream memory:

```python
# Via MCP tool with explicit project param:
mem_search(query: "Mercadona pattern", project: "serendipilink", limit: 3)
# Then read full content of any hit:
mem_get_observation(id: <id_from_search>)
```

Or via CLI:

```bash
engram search "Mercadona pattern" --project serendipilink --limit 3
```

**All upstream drydock-related memories live under `project=serendipilink`**
in the host engram (the drydock project namespace is new and empty until
this session starts saving to it). Listed below with topic_keys to make
searches deterministic:

### Drydock architecture context

- **memory #919** (topic `infra/drydock/repo-and-release-plan`) — this
  briefing's context: the migration from `~/drydock/` to `~/git/drydock/`,
  initial commit 83bb349, why no public push yet.
- **memory #918** (topic `infra/drydock/cli-and-multi-project`) — the CLI
  evolution from claude-sandbox to drydock + multi-project parametrization.
- **memory #916** (topic `infra/claude-sandbox/architecture`) — the original
  claude-sandbox architecture before the drydock rename. Still valid for
  understanding load-bearing decisions (engram split, hooks RO).
- **memory #915** (topic `personal/claude-code-hooks/block-destructive`) —
  hook hardening that drydock RO-mounts (tier 1 of defense-in-depth).

### CLAUDE.md style pattern (Mercadona)

- **memory #870** (session_summary 2026-05-09) — full context of the
  CLAUDE.md refactor in serendipilink that applied the Mercadona pattern.
  Documents the exact pattern (rule + WHY + consequence) and the rationale
  for distinguishing "documentation" from "constraints".
- **memory #872** (session_summary 2026-05-09 follow-up) — the next session
  that continued applying the pattern to Stack Versions table + CI Gates.
- **Reference file** (only on host, NOT in drydock container):
  `/home/rai/git/serendipilink/CLAUDE.md` (291 lines) — the canonical
  real-world example. Readable only if you run this SDD on host Claude. From
  inside the drydock container, use the worked example in the "CLAUDE.md
  content guide" section instead, plus `mem_get_observation(id: 870)` /
  `(id: 872)` (those memories are in the container's engram DB copy).

When the new session is inside `~/git/drydock/`, engram auto-detects
project=`drydock` for new saves. The above memories stay tagged as
`serendipilink` (intentional — they document the upstream context). New
drydock-specific architectural decisions made in the next session should
be saved with `--project drydock` (or rely on auto-detection from cwd).

**Bottom line on context access from inside the drydock container**:
- ✓ `docs/v0.1.0-release-plan.md` (this file) — mounted with the project.
- ✓ `mem_get_observation(id: 870/872/915)` — those predate the engram-container
  init copy, so they're in `~/.engram-container/engram.db`.
- ✗ `mem_get_observation(id: 916/918/919)` — saved AFTER the init copy; NOT
  in the container DB unless you re-sync (`cp ~/.engram/engram.db
  ~/.engram-container/engram.db` on host first — mind the WAL caveats).
- ✗ `/home/rai/git/serendipilink/CLAUDE.md` — not mounted; host-only.
- ✓ `mem_search(query, project: "serendipilink")` — works (queries the
  container DB), but only finds the pre-init-copy subset.

If you need the newer memories or the serendipilink file: run this SDD on
**host Claude** instead of the drydock container. You lose dogfooding, gain
full context access. Your call. **The briefing is self-sufficient either way.**

## How to start the next session

> **Where things stand**: #0/#1/#1.5/#1.7/#2/#3/#4/#5 are landed on `main` (HEAD
> `ac656e8`, `bats test/` 150/150, nothing pushed). `sdd-init` for this project
> has already been run and is persisted in engram (`sdd-init/drydock` #920) —
> do NOT re-run it. The next change is **#6 `examples-directory`**. See
> "Progress" above.

```bash
cd ~/git/drydock

# In host Claude OR drydock-containerized Claude:
# 1. Recover context (sdd-init already done — just pull the project context):
#      mem_context  +  mem_search "sdd-init/drydock"  → mem_get_observation
#      mem_search "v0.1.0 release plan progress"  /  read docs/v0.1.0-release-plan.md
#    Engram quirk: omit the `project` param on mem_* calls — passing
#    project: "drydock" fails ("unknown_project"); it auto-detects from git_root.
# 2. Start the next SDD change (#6 — `examples/web-stack/` and `examples/minimal/`, both demonstrating drydock against a fresh project):
/sdd-new examples-directory
```

SDD config used so far (keep unless you decide otherwise): artifact store =
engram; delivery strategy = ask-on-risk; execution mode = interactive.
Project rules: conventional commits; NO `Co-Authored-By` / AI attribution; never
`git commit/push --no-verify`; branch first if on `main`; do NOT push (no
GitHub remote). Tools (not installed locally): `npx --yes bats@1.13.0 test/`,
`npx --yes shellcheck …`; shfmt has no npm package — `curl -fsSL -o /tmp/shfmt
https://github.com/mvdan/sh/releases/download/v3.8.0/shfmt_v3.8.0_linux_amd64
&& chmod +x /tmp/shfmt`. After any change: `bats test/` must stay green and
`shellcheck bin/drydock lib/*.sh` clean.

Or, hand the session the prompt below verbatim (it's stale on a few points now
that #0–#1.7 are done — the bracketed notes correct it).

---

## Suggested opening prompt for the next session

> [STALE — corrections inline] **#0/#1/#1.5/#1.7/#2/#3/#4/#5 are DONE and on `main`
> (HEAD `ac656e8`); `sdd-init` for this project is already done and persisted
> in engram — do NOT re-run it; start the next SDD change at **#6
> `examples-directory`**. Engram quirk: omit the `project` param on
> `mem_*` calls.]
>
> I'm working on `drydock`, a containerized Claude Code sandbox. The full
> context, current state, constraints, and release plan are in
> `docs/v0.1.0-release-plan.md`. Please read that file first — entire (esp. the
> "Progress" section).
>
> The goal is to take drydock from "works for one user (me)" to "ready to
> publish at `sideralith/drydock` on GitHub". SDD changes listed in the plan
> with estimates.
>
> ~~Start by running `/sdd-init`~~ [already done — skip]. Before any code
> change, recall context (`mem_context`, `mem_search "sdd-init/drydock"` →
> `mem_get_observation`, read this briefing). **Important**: upstream
> serendipilink memories live under
> `project=serendipilink` — pass `project: "serendipilink"` explicitly to
> `mem_search` (engram has no cross-project search flag in v1.15.10):
>
> - `mem_search(query: "drydock", project: "serendipilink")` — architecture history
> - `mem_search(query: "Mercadona", project: "serendipilink")` — CLAUDE.md pattern
>
> The briefing lists specific memory IDs to `mem_get_observation` (those work
> by ID without a project filter; #870/#872/#915 are in the container engram
> DB, #916/#918/#919 are not — see the "Bottom line on context access"
> section in the briefing).
>
> Note: if you're running this inside a drydock container, the file
> `/home/rai/git/serendipilink/CLAUDE.md` is NOT accessible (other project
> trees aren't mounted). The briefing's "CLAUDE.md content guide" section has
> the pattern + a worked example — that's self-sufficient. If you want the
> verbatim serendipilink file, run this SDD on host Claude instead.
>
> Suggested order (remaining):
> ~~1. `parameterize-paths`~~ — DONE (`main` @ `39bb8ca`).
> ~~2. `engram-optional`~~ — DONE (`main` @ `ba74b47`; engram optional + conditional overlay + opt-in shared topology with WSL2/macOS safety gate).
> ~~3. `claude-md`~~ — DONE (`main` @ `d221600`; AI-agent constitution at repo root, 220 lines, Mercadona pattern, 7 architectural invariants + tooling conventions + threat model boundary + tracking & contribution + personality).
> ~~4. `add-license-contributing`~~ — DONE (`main` @ `dc29e52`; CONTRIBUTING.md extended with PR flow + code-style + engram team-memory pattern, 196 lines).
> ~~5. `install-script`~~ — DONE (`main` @ `ac656e8`; install.sh 249 lines + bats 150/150 + curl-pipe + manual install paths + TTY-aware output + idempotent re-install).
> 6. `examples-directory` — examples/web-stack/ + examples/minimal/.
> 7-10. Continue per the SDD breakdown table.
>
> Honor the constraints section strictly. Threat model is "defense against
> agent accidents, not adversarial" — don't add socket proxy or other v0.2.0
> work in this round.
>
> Verify each change by:
> 1. Re-running `drydock` against `~/git/serendipilink` (regression — must
>    still work end-to-end).
> 2. Re-running `drydock init /tmp/test-X && cd /tmp/test-X && drydock
>    status` (clean-state check).
> 3. For doc-only changes (e.g. #4 CONTRIBUTING extension), additionally:
>    cold-read the file imagining no drydock knowledge — can an edge case be
>    judged from the file alone? Iterate until yes.

---

## Validation gate (run after each change)

```bash
# Regression: drydock still works against the original test project
cd ~/git/serendipilink && drydock status   # should show all green
cd ~/git/serendipilink && drydock doctor   # should detect 9P docs/

# Clean state: drydock works from a fresh project dir
tmpdir=$(mktemp -d)
cd "$tmpdir" && git init && drydock init . && drydock status
# expected: settings.json created with substituted $HOME; status all green

# Shellcheck (when point 5 is done)
shellcheck bin/drydock install.sh
```

## Known unknowns to investigate during work

- Does Docker Desktop on Mac expose `/var/run/docker.sock` symlinked?
  (Check on a Mac if you have access; otherwise document as "untested".)
- gRPC FUSE perf for the project mount on Mac. (Document if dramatic.)
- Does `engram` binary work on macOS (Intel + ARM)? Yes — engram is a
  cross-platform Go binary (`CGO_ENABLED=0`, pure-Go SQLite). GitHub Releases
  ship native macOS Mach-O builds (Intel + Apple Silicon). Homebrew formula:
  `brew install gentleman-programming/tap/engram`. The **DB directory**
  (`~/.engram/`) can be bind-mounted on macOS; however, the **macOS Mach-O
  binary cannot run inside the Debian Linux container**. drydock v0.1.0 treats
  macOS as engram-effectively-absent: the overlay is not activated, and the
  MCP server is filtered out so Claude Code sees no startup error. Future
  options: ship a `linux_amd64` engram in the image, or bridge via its HTTP
  API on port 7437. Neither is in v0.1.0.

---

*This document is a one-shot briefing. Once v0.1.0 is released, archive it
under `docs/releases/v0.1.0-plan.md` (historical) and create
`docs/releases/v0.2.0-plan.md` for the next round.*
